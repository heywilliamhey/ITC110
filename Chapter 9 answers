Practice Questions
1. What is the difference between shutil.copy() and shutil.copytree()?
shutil.copy()  will coppy a single file
shutil.copytree() will copy a file and its contents

2. What function is used to rename files?
shutil.move()  renames and will moves files
3. What is the difference between the delete functions in the send2trash
and shutil modules?
send2trash moves a file to the recycle bin
shutil  will permanently delete a file
4. ZipFile objects have a close() method just like File objects’ close()
method. What ZipFile method is equivalent to File objects’ open()
method?
Zipfile.zipfile() is equivalent to the open() method
